{
  "entities": {
    "leaderboard": {
      "title": "Leaderboard Entry",
      "description": "Global submarine depth records",
      "type": "object",
      "properties": {
        "playerName": { "type": "string" },
        "depth": { "type": "number" },
        "coinsGained": { "type": "number" },
        "date": { "type": "string" },
        "subColorIndex": { "type": "number" },
        "createdAt": { "type": "string" }
      },
      "required": ["playerName", "depth", "coinsGained", "date"]
    },
    "history": {
      "title": "History Log",
      "description": "Submarine dive history logs",
      "type": "object",
      "properties": {
        "playerName": { "type": "string" },
        "depth": { "type": "number" },
        "coinsGained": { "type": "number" },
        "reason": { "type": "string" },
        "date": { "type": "string" },
        "time": { "type": "string" },
        "createdAt": { "type": "string" }
      },
      "required": ["playerName", "depth", "coinsGained", "reason"]
    }
  }
}
